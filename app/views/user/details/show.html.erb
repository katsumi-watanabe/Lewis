<div class="sneaker-width-1">
  <h2><%= @user.name %>さん詳細ページ</h2>
  <div class="d-flex">
    <div class="mr-4">
      <%= attachment_image_tag @user, :profile_image, format: 'jpeg', fallback: "pagu.jpeg", class: "comment-user-image-2" %>
    </div>
    <div class="ml-4">
      <div class="d-flex my-3">
        <p><%= @user.name %></p>
        <% if current_user.id == @user.id %>
          <p class="ml-4"><%= link_to "プロフィール編集", edit_user_detail_path(current_user), class: "btn btn-outline-dark" %></p>
        <% end %>
      </div>
      <div class="d-flex">
        <p>投稿件数<%= @user.post_sneakers.count %>件</p>
        <%= link_to followings_path(@user) do %>
        <p class="ml-5">フォロー<%= @user.followings.count %>人</p>
        <% end %>
        <%= link_to followers_path(@user) do %>
        <p class="ml-5">フォロワー<%= @user.followers.count %>人</p>
        <% end %>
      </div>
    </div>
  </div>

  <p class="my-5 ml-3 border-bottom"><%= @user.introduction %></p>

  <% if @post_sneakers.present? %>
    <% @user.post_sneakers.each do |post_sneaker| %>
      <%= link_to post_sneaker_path(post_sneaker.id) do %>
        <%= attachment_image_tag post_sneaker, :post_image, :fill, 260, 260, format: 'jpeg', class: "sneaker-image" %>
      <% end %>
    <% end %>
  <% else %>
    <p>投稿はありません</p>
  <% end %>
</div>